# 📈 股價與數據監控儀表板 (Stock & Data Monitor)

這是一個自動化的數據監控系統。
- **大腦 (GitHub Actions)**: 每天早上 7:05 (台灣時間) 自動抓取數據並儲存。
- **臉孔 (Streamlit Cloud)**: 讀取數據並畫出互動式圖表。

## 檔案說明
- `scraper.py`: 爬蟲程式，負責產生數據。
- `app.py`: 儀表板網站程式。
- `monitoring_list.csv`: 要監控的項目清單。
- `history_data.csv`: 歷史數據儲存檔 (自動產生)。
- `.github/workflows/daily_scrape.yml`: 自動化排程設定。

## 🚀 部署教學 (Deployment Steps)

### 第一步：上傳到 GitHub
1.  **登入 GitHub** 並點選右上角的 **+** -> **New repository**。
2.  **Repository name** 取名為 `stock_dashboard` (或其他你喜歡的名字)。
3.  勾選 **Public** (公開) 或 **Private** (私有) 皆可 (建議 Public 方便 Streamlit 讀取)。
4.  點選 **Create repository**。
5.  在你的電腦上，將這些檔案上傳到該 Repository。
    - 如果你使用網頁版 Upload files，請直接拖曳所有檔案 (包含 `.github` 資料夾)。

### 第二步：設定 GitHub Actions 權限
1.  進入你的 GitHub Repository 頁面。
2.  點選 **Settings** > **Actions** > **General**。
3.  滑到最下方 **Workflow permissions**。
4.  勾選 **Read and write permissions** (這很重要！不然機器人無法存檔)。
5.  點選 **Save**。
6.  (測試) 你可以到 **Actions** 頁籤，點選 **Daily Scrape**，然後按 **Run workflow** 手動跑一次測試。

### 第三步：開啟 Streamlit Cloud
1.  前往 [Streamlit Cloud](https://streamlit.io/cloud) 並登入 (使用 GitHub 帳號)。
2.  點選 **New app**。
3.  **Repository**: 選擇剛剛建立的 `stock_dashboard`。
4.  **Branch**: 通常是 `main`。
5.  **Main file path**: 選擇 `app.py`。
6.  點選 **Deploy!**

完成！現在你每天早上起床，網頁上的圖表就會自動更新了！🎉
